config_name: hyper_unet, channels=4
description: "hyper config, channels=4"

n_channels: 4
n_classes:
ph_n: 4  # Setting the Kronecker Decomposition Dimension
n_layers: 5
epochs: 50


cuda: True

dataset_name: modl_dataset
dataset_params:
  dataset_path: 'data/dataset.hdf5'
  sigma: 0.01
val_data: False
batch_size: 1

model_name: base_modl

restore_weights: False #False, best, inter, final
restore_path:

loss_name: MSE
score_names: ['PSNR', 'SSIM']
optim_name: Adam
optim_params:
  lr: 0.001
gradient_clip: True